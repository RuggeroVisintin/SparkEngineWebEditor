declare module "sparkengineweb" {
    // Intentionally export only this marker so any real import fails type-check.
    export type __USE_AT_SPARKENGINE_INSTEAD__ = "Use '@sparkengine' instead of 'sparkengineweb'.";
}
